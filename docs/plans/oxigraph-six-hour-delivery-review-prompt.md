SIX-HOUR OXIGRAPH DELIVERY REVIEW (v2)

Goal: deliver the usable, tested Oxigraph R1 release defined by ADR-0043. This review must shorten the path to delivery. R1 completion requires the write interface and audited upstream integration, affected tests passing, truthful release documentation, a reproducible submodule graph, and authorized publication. An unresolved release hold is not completion. Preserve the wider programme backlog; do not silently remove features or add release prerequisites.

1. Recover current state. First use live structured Ruflo MCP to retrieve programme-controls/oxigraph-six-hour-delivery-course-correction-v1, the active delivery task, and the previous verified review. The current R1 task is task-1788770182100-hyaa2v; follow a successor only if explicitly recorded. Verify the canonical main checkout and read AGENTS.md, ADR-0043, and relevant changed sections of docs/plans/persistence-write-and-linked-data-parity-plan.md and docs/plans/linked-data-store-evolution-harness-plan.md. Preserve existing work and use one Git writer.

2. Review for at most 10 minutes, then continue delivery work. Record the exact preceding six-hour interval, HEAD, relevant commits, working-tree changes, and test commands/results. Distinguish a manual wake-up test from a natural timer firing; do not count overlapping review evidence twice. Compare the previously selected outcome with what actually finished. Report separately: user-visible behavior delivered; required release gates newly closed; and documentation, harness, research, planning, or receipt work. Repeated passing tests and task counts are not new product progress. Mark missing evidence unknown; do not invent effort totals or percentages.

3. Run one read-only Ruflo audit through hooks_worker_dispatch. Record its exact ID and the daemon audit counter before and after using MCP. Poll within a 60-second window while doing useful work; inspect any returned result against the repository evidence. Queued, pending, synthetic-completed, or a counter increase alone do not prove that exact audit ran. Report disagreements; cancel only this review's still-pending job and continue the review directly. A failed audit or memory tool is not a product blocker. Do not repair the scheduler, daemon, or harness during the review unless its failure directly prevents delivery.

4. Course-correct and execute. If no product behavior or required release gate advanced, name the concrete cause and stop the non-gating activity responsible. Choose the single remaining step with the greatest effect on delivery, define its observable completion check, and implement it now. If progress was good, continue the next required step anyway. Prefer an existing product test or removal of a demonstrated build/release blocker. Keep non-gating harness work frozen; use independent read-only parallel work only when it accelerates this step. Run proportionate tests, commit each verified slice to main, and continue through authorized dependent steps. Do not stop after producing a report or plan.

5. Persist and report. Update the live delivery task and a concise repository-memory review containing the interval, commits, exact tests, gates closed, blocker, correction, and next action. Read back the exact stored value. Keep task completion tied to evidence. Report a calendar ETA only when measured remaining work supports it; otherwise report the remaining gates. Apply existing publication authorization without repeatedly asking, but an unresolved authority gap requires the exact missing decision after all independent authorized work is ready.

6. Respect boundaries and completion. Always use Ruflo through MCP. Obey main-only, one-writer, immutable-evidence, native-subscription, and publication rules in AGENTS.md. Do not run G1.7 or provider-backed qualification without exact authority; never use OpenRouter or model-provider API keys. If the required native subscription or requested model is unavailable, pause and report the exact client, model, and error. Once every R1 delivery gate is verified, report completion and disable this R1 review timer. Completion of R1 does not imply completion of the wider roadmap.
